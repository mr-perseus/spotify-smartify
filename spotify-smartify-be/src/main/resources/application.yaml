spring:
  application:
    name: spotify-smartify-be

spotify:
  client-id: ${SPOTIFY_CLIENT_ID}
  client-secret: ${SPOTIFY_CLIENT_SECRET}
  redirect-uri: ${SPOTIFY_REDIRECT_URI:http://127.0.0.1:8080/auth/callback}

app:
  frontend-url: ${FRONTEND_URL:http://127.0.0.1:3000}
